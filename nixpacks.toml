[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --only=production || npm install --only=production"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "node server.js"
