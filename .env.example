# WormGPT Qwen API - Railway Deployment Configuration
# Copy this file to .env and configure for your deployment

# === Server Configuration ===
PORT=4100
NODE_ENV=production

# === Domain Configuration ===
# Set these for Railway deployment
DOMAIN=your-project.railway.app
PROTOCOL=https
PUB_PORT=443

# === OpenRouter API Key (Required for Qwen Models) ===
# Get your free API key from https://openrouter.ai/
OPENROUTER_API_KEY=your_openrouter_api_key_here

# === Optional: Direct Qwen API (if available) ===
# QWEN_API_KEY=your_qwen_api_key_here

# === Security & Sessions ===
JWT_SECRET=generate_a_random_secret_here_min_32_chars
COOKIE_NAME=wormgpt-session

# === AI Configuration ===
# Default model for Qwen API
DEFAULT_QWEN_MODEL=qwen/qwen-3-4b:free

# === Rate Limiting (Optional) ===
# RATE_LIMIT_REQUESTS_PER_MINUTE=60
# RATE_LIMIT_MAX_REQUESTS=1000

# === CORS Configuration ===
# Allow all origins for direct integration (set to your domain in production)
CORS_ORIGIN=*

# === Logging ===
LOG_LEVEL=info

# === Database (Optional - for persistent sessions) ===
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# === Redis (Optional - for caching) ===
# REDIS_URL=redis://localhost:6379

# === Feature Flags ===
# Disable captcha for direct API integration
CAPTCHA_ENABLED=false
# Allow anonymous API access
ALLOW_ANONYMOUS_API_ACCESS=true
# Disable temporary users if not needed
DISABLE_TEMP_USERS=true
